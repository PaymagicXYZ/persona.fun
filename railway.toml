[build]
builder = "nixpacks"
buildCommand = "bun install && cd apps/eliza && bun run build"

[deploy]
startCommand = "cd apps/eliza && bun run start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure" 