[phases.setup]
nixPkgs = ['bun']

[phases.install]
cmds = ['bun install']

[phases.build]
cmds = [
  'bun run build',
  'cd apps/eliza && bun run build'
]

[start]
cmd = 'cd apps/eliza && bun run start' 