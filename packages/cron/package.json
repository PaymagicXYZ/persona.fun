{
  "name": "@persona/cron",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "types": "./src",
  "main": "src/update-merkle-tree.ts",
  "files": ["types", "dist"],
  "scripts": {
    "update-merkle-tree": "bun run src/update-merkle-tree.ts",
    "start": "bun run update-merkle-tree",
    "railway:build": "tsup src/update-merkle-tree.ts --format esm --dts --out-dir dist"
  },
  "dependencies": {
    "@persona/db": "*",
    "@persona/utils": "*",
    "@farcaster/hub-nodejs": "^0.12.7",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "bun-types": "latest",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
