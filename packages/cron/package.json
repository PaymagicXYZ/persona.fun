{
  "name": "@persona/cron",
  "version": "0.0.1",
  "private": true,
  "types": "./src",
  "main": "src/index.ts",
  "files": ["types", "dist"],
  "scripts": {
    "update-merkle-tree": "bun run src/update-merkle-tree.ts",
    "build": "tsup",
    "railway:build": "tsup src/index.ts --format esm --dts --out-dir dist",
    "start": "bun run update-merkle-tree"
  },
  "dependencies": {
    "@persona/db": "*",
    "@persona/utils": "*",
    "@farcaster/hub-nodejs": "^0.12.7",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "bun-types": "latest",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
